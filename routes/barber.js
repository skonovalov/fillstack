//const barbers = require('../db');

module.exports = (router) => {
	/* router.get('/barber/:id', (req, res, next) => {
		let id = req.params.id;
	
		barbers.find((item) => {
			if (item.id == id) {
				res.render('barber', {item});
			}
		});

		next();
	}); */
};